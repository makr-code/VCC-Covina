# VCC-Covina Main Backend Deployment
# Phase 1: Foundation Enhancement
apiVersion: apps/v1
kind: Deployment
metadata:
  name: covina-main-backend
  namespace: covina
  labels:
    app.kubernetes.io/name: covina
    app.kubernetes.io/component: main-backend
    app.kubernetes.io/version: "3.4.11"
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: covina
      app.kubernetes.io/component: main-backend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: covina
        app.kubernetes.io/component: main-backend
        app.kubernetes.io/version: "3.4.11"
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "45678"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: covina-service-account
      containers:
      - name: main-backend
        image: registry.local/covina/main-backend:3.4.11
        imagePullPolicy: Always
        ports:
        - containerPort: 45678
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: covina-config
        - secretRef:
            name: covina-secrets
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 45678
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 45678
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
        volumeMounts:
        - name: tmp-volume
          mountPath: /tmp
        - name: data-volume
          mountPath: /app/data
      volumes:
      - name: tmp-volume
        emptyDir: {}
      - name: data-volume
        persistentVolumeClaim:
          claimName: covina-data-pvc
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                  - main-backend
              topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: covina-main-backend
  namespace: covina
  labels:
    app.kubernetes.io/name: covina
    app.kubernetes.io/component: main-backend
spec:
  type: ClusterIP
  ports:
  - port: 45678
    targetPort: 45678
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: covina
    app.kubernetes.io/component: main-backend
