# Recommended runtime dependencies for database adapters
requests>=2.28.0
pg8000>=1.29.2
# Optional: binary psycopg2 (may require wheel/binaries on Windows)
psycopg2-binary>=2.9.7

# WebSocket Client für Real-Time Updates
websocket-client>=1.6.0

# FastAPI Production Server (Multi-Worker)
gunicorn>=21.2.0  # WSGI Server mit Worker Management
uvicorn[standard]>=0.24.0  # ASGI Server mit HTTP/2 Support

# Web Scraping & HTML Parsing
beautifulsoup4>=4.12.0  # HTML/XML Parser für Handelsregister
lxml>=4.9.0  # Schneller HTML Parser (optional)

# AI/ML Dependencies for Advanced Content Processing
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0
spacy>=3.6.0
scikit-learn>=1.3.0
nltk>=3.8.0
numpy>=1.24.0
pandas>=2.0.0

# Graph-RAG Dependencies (Phase 1 - Hybrid Retrieval)
rank-bm25>=0.2.2  # BM25 Keyword Search
neo4j>=5.14.0  # Neo4j Driver für Graph Context

# Admin Dashboard & Monitoring Dependencies
matplotlib>=3.7.0  # Visualization Charts
rich>=13.0.0  # Terminal UI
psutil>=5.9.0  # System Metrics (optional)

# Optional GPU support (uncomment for CUDA)
# torch-audio>=2.0.0
# torchaudio>=2.0.0
